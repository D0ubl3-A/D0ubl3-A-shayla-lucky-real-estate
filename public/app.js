(()=>{const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];const q=new URLSearchParams(location.search),tracking={utm_source:q.get('utm_source')||'',utm_medium:q.get('utm_medium')||'',utm_campaign:q.get('utm_campaign')||'',referrer:document.referrer||'',source_page:location.pathname};$$('.intent-tab').forEach(b=>b.addEventListener('click',()=>{const f=b.closest('form');$$('.intent-tab',f).forEach(x=>x.classList.remove('active'));b.classList.add('active');const i=$('input[name="intent"]',f);if(i)i.value=b.dataset.intent;}));$$('form[data-lead-form]').forEach(f=>f.addEventListener('submit',async e=>{e.preventDefault();const s=$('.form-status',f),btn=$('button[type="submit"]',f);s.className='form-status';s.textContent='Sending your request…';btn.disabled=true;const payload=Object.fromEntries(new FormData(f).entries());Object.assign(payload,tracking);try{const r=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Unable to send request.');s.classList.add('ok');s.textContent='Received. Your private request has been saved for follow-up.';f.reset();}catch(err){s.classList.add('err');s.textContent=err.message||'Unable to send request right now.';}finally{btn.disabled=false;}}));$$('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=$(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'});}}));})();